{
  "name": "com7-lark-sync",
  "version": "0.1.0",
  "description": "Com7 → LarkBase one-way sync/reconciliation engine (Express + MySQL, clean architecture)",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test",
    "migrate": "node src/infrastructure/database/migrate.js",
    "worker": "node src/worker.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "mysql2": "^3.22.5"
  }
}
